# Example .env for AutoPatch-PR-Agent
# Keep secret tokens out of source control. Copy to `.env` and fill values.

# GitHub personal access token (only required if running CLI; UI asks for token too)
GITHUB_TOKEN=

# Google API key for GenAI (if using google-genai)
GOOGLE_API_KEY=

# Optional defaults
DEFAULT_BASE_BRANCH=main
TEMP_REPOS_DIR=./temp_repos
MODEL_NAME=gemini-2.0-flash
APP_NAME=auto-patch-pr-agent
